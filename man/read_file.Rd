% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/files.R
\name{read_file}
\alias{read_file}
\title{Read a file}
\usage{
read_file(f, fn = NULL, msg = TRUE, ...)
}
\arguments{
\item{f}{Path to a file.}

\item{fn}{Optional reader function. If `NULL`, `read_file()` chooses a
reader based on the file extension.}

\item{msg}{Should the function message which file it is reading?}

\item{...}{Additional arguments passed to the reader.}
}
\value{
The parsed file contents. Usually a data frame, but this depends on
  the file type and reader.
}
\description{
Read a file into R.
}
\details{
`read_file()` supports common text, spreadsheet, and statistical data
formats, including `.csv`, `.xlsx`, `.dta`, `.sav`, `.rds`, and `.RData`.
It prefers package-specific readers when available and falls back to base R
where possible.
}
\examples{
\dontrun{
df <- read_file("data/my_data.csv")
x <- read_file("data/my_data.dta")
}
}
\seealso{
[write_file()], [read_survey_data()]
}
