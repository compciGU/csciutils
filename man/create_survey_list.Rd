% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_utils.R
\name{create_survey_list}
\alias{create_survey_list}
\title{Create a named list of survey datasets from the database}
\usage{
create_survey_list(
  conn,
  proj = NULL,
  include_dataset_id = TRUE,
  use_original = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{conn}{A DBI connection.}

\item{proj}{Character vector or `NULL`. If supplied, only rows where
`datasets$proj` matches one of these values are kept (case-insensitive).
Examples: `"eb"`, `"ess"`, `c("eb","ess")`. Default `NULL`.}

\item{include_dataset_id}{Logical. Add a `dataset_id` column to each loaded
dataset? Default `TRUE`.}

\item{use_original}{Logical. Passed to [read_survey_data()]. Default `TRUE`.}

\item{verbose}{Logical. Print progress messages? Default `TRUE`.}
}
\value{
A named list of survey data frames, keyed by `tag`. Failed loads are
  stored as `NULL`.
}
\description{
Queries the `datasets` table, optionally filters to one or more projects via
the `proj` column, then reads each survey dataset via [read_survey_data()].
The result is returned as a named list keyed by `tag`. Optionally appends
`dataset_id` to each loaded dataset.
}
\examples{
\dontrun{
surveys <- create_survey_list(conn, proj = "eb")
}
}
