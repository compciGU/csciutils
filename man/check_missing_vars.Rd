% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cwt_align_vars.R
\name{check_missing_vars}
\alias{check_missing_vars}
\title{Check for variables in variable_map not found in the CWT}
\usage{
check_missing_vars(cwt, variable_map, verbose = TRUE)
}
\arguments{
\item{cwt}{A CWT data frame as returned by [create_cwt()].}

\item{variable_map}{A variable mapping data frame with at least a `src_var`
column.}

\item{verbose}{Logical. Print a message if missing variables are found?
Default `TRUE`.}
}
\value{
Called for its side effect. Returns `NULL` invisibly.
}
\description{
Compares `src_var` entries in `variable_map` against `var_name` entries in
the CWT and prints a note for any that are missing. Useful for catching
typos in `src_var` names.
}
\keyword{internal}
