% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotation_utils.R
\name{query_annotations}
\alias{query_annotations}
\title{Read and reshape a variable annotation sheet from the database}
\usage{
query_annotations(
  conn,
  table,
  id_cols = c("dataset_id", "study_wave"),
  split_separators = "\\\\s*[;,]\\\\s*",
  format = c("long", "wide"),
  verbose = TRUE
)
}
\arguments{
\item{conn}{A DBI connection.}

\item{table}{Character. Annotation table name in the database
(e.g. `"cses_src_annotations"`).}

\item{id_cols}{Character vector. Columns to keep as identifiers (not pivoted).
Default `c("dataset_id", "study_wave")`.}

\item{split_separators}{Character. Regular expression of separators used to
detect and split multiple values in a cell. Default splits on `;` or `,`.}

\item{verbose}{Logical. Print progress messages? Default `TRUE`.}
}
\value{
A long annotation data frame with columns `dataset_id`, `study_wave`,
  `target_var`, and `src_var`.
}
\description{
Queries an annotation table from the database and reshapes it to long format.
The returned data frame contains one row per `(dataset_id, study_wave,
target_var, src_var)` mapping. Cells containing multiple source variables are
automatically split into multiple rows when separated by common delimiters
(e.g. `";"` or `","`).
}
\examples{
\dontrun{
cses_annotations_long <- query_annotations(
  conn,
  table = "cses_src_annotations"
)
}
}
