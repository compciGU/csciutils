% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{add_scales_to_questions}
\alias{add_scales_to_questions}
\title{Join Questions with Scales}
\usage{
add_scales_to_questions(conn, inner_join = FALSE)
}
\arguments{
\item{conn}{A DBI database connection object}

\item{inner_join}{Logical. If TRUE, uses INNER JOIN to return only questions 
with scales. If FALSE (default), uses LEFT JOIN to return all questions.}
}
\value{
A data frame with combined question and scale information
}
\description{
Retrieves all questions and their associated scales from the database.
Uses a LEFT JOIN to include questions that don't have scales.
}
\examples{
\dontrun{
conn <- connect_db()
questions_scales <- add_scales_to_questions(conn)
DBI::dbDisconnect(conn)
}
}
