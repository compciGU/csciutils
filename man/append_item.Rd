% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cwt_append.R
\name{append_item}
\alias{append_item}
\title{Append missing variables to an existing CWT}
\usage{
append_item(
  cwt,
  survey,
  vars_df,
  save_file = TRUE,
  dir = getwd(),
  out_dir = "cwt_swd/cwts_appended",
  align_surveys_with_cwt = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{cwt}{Existing CWT data frame as returned by [create_cwt()].}

\item{survey}{Named list of survey data frames.}

\item{vars_df}{Variable mapping data frame with columns `src_var`,
`target_var`, and `study_wave`.}

\item{save_file}{Logical. Save the result to an xlsx file? Default `TRUE`.}

\item{dir}{Root directory for the output file. Default `getwd()`.}

\item{out_dir}{Sub-directory relative to `dir`. Created if absent.}

\item{align_surveys_with_cwt}{Logical. Subset `survey` to waves found
in `cwt`? Default `FALSE`.}

\item{verbose}{Logical. Print progress messages? Default `TRUE`.}
}
\value{
Updated CWT data frame.
}
\description{
Identifies variables present in `vars_df` but absent from `cwt`,
builds CWT rows for them, and appends them to the existing CWT.
}
\examples{
\dontrun{
  cwt_updated <- append_item(cwt, my_survey_list, new_vars_df)
}
}
