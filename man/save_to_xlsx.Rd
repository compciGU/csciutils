% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cwt_write.R
\name{save_to_xlsx}
\alias{save_to_xlsx}
\title{Save a data frame to an xlsx file}
\usage{
save_to_xlsx(
  data,
  output_dir = getwd(),
  output_subdir = NULL,
  file_prefix = NULL,
  file_suffix = "_cwt_appended",
  study_wave = NULL,
  overwrite = TRUE
)
}
\arguments{
\item{data}{A data frame to save.}

\item{output_dir}{Root output directory. Default `getwd()`.}

\item{output_subdir}{Sub-directory relative to `output_dir`. Set to `NULL`
to save directly in `output_dir`. Created automatically if absent.}

\item{file_prefix}{Character. String prepended to the file name. Derived
from `study_wave` if `NULL`. Default `NULL`.}

\item{file_suffix}{Character. String appended to the file name before the
`.xlsx` extension. Default `"_cwt_appended"`.}

\item{study_wave}{Character. A study wave string (e.g. `"lits_3"`) used to
derive `file_prefix` when `file_prefix` is `NULL`. Default `NULL`.}

\item{overwrite}{Logical. Overwrite an existing file? If `FALSE` and the
file already exists, an error is raised. Default `TRUE`.}
}
\value{
The full file path, invisibly.
}
\description{
A general-purpose xlsx writer with flexible file naming. The file name is
built as `{file_prefix}{file_suffix}.xlsx`. Either `file_prefix` or
`study_wave` must be supplied so the name can be resolved.
}
\keyword{internal}
