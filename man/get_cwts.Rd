% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_utils.R
\name{get_cwts}
\alias{get_cwts}
\title{Read CWT tables from disk into a named list}
\usage{
get_cwts(
  dir_cwts = file.path(Sys.getenv("ROOT_DIR"), "cwt_swd", "cwt_swd_aligned"),
  proj = NULL,
  pattern = "\\\\.(xlsx|csv|ods)$",
  name_extension = FALSE,
  to_global_env = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{proj}{Character vector or `NULL`. If supplied, only files whose base
name begins with one of these project prefixes followed by `"_"` are read
(case-insensitive). Examples: `"evs"`, `"ess"`, `c("evs","ess")`.
Default `NULL`.}

\item{pattern}{Character. File extension pattern to include. Default matches
`.xlsx`, `.csv`, and `.ods`.}

\item{to_global_env}{Logical. Assign the list elements into `.GlobalEnv`?
Default `FALSE`.}

\item{verbose}{Logical. Print progress messages? Default `TRUE`.}

\item{dir}{Root directory containing CWT files. Default resolved from
`Sys.getenv("ROOT_DIR")` and `"cwt_swd/cwt_swd_aligned"`.}

\item{full_names}{Logical. Return full paths in the `names()` of the list?
Default `FALSE`.}
}
\value{
A named list of CWT tables.
}
\description{
Reads CWT files from a directory and returns them as a named list keyed by
file name (without extension). Optionally filters the files to read based on
a project prefix found in the file name (e.g. `"evs"` in
`"evs_cwt_appended.csv"`). Supports `.xlsx`, `.csv`, and `.ods` files via
[read_file()].
}
\examples{
\dontrun{
cwts <- read_cwts(proj = "evs")
}
}
