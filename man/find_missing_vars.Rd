% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cwt_align_vars.R
\name{find_missing_vars}
\alias{find_missing_vars}
\title{Identify variable_map rows whose variables are absent from the CWT}
\usage{
find_missing_vars(cwt, variable_map, has_waves)
}
\arguments{
\item{cwt}{A CWT data frame with columns `var_name` and `study_wave`.}

\item{variable_map}{A variable mapping data frame with columns `src_var`
and `study_wave`.}

\item{has_waves}{Logical. Whether the CWT spans multiple files or waves.}
}
\value{
A subset of `variable_map` containing only rows for variables not
  yet present in the CWT.
}
\description{
When the CWT spans multiple files (`has_waves = TRUE`), matching is done on
both `src_var` and `study_wave` to avoid false positives where the same
variable name appears in different waves. Otherwise matching is on
`src_var` alone.
}
\keyword{internal}
